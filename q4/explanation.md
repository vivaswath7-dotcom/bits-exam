# Q4 Explanation

This question investigated file descriptors, redirection, and shell resource limits. File descriptors are the mechanism Linux uses to represent open files, and redirection controls where process input and output streams are sent.
