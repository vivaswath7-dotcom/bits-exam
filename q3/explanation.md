# Q3 Explanation

This question demonstrated the difference between a hard link and a symbolic link. Hard links point to the same inode, so they remain available after the original file is deleted, while symbolic links depend on the target path and break when that target is removed.
